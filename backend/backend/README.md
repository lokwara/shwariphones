# Backend API

## Run

```bash
npm run dev
```

## Sentry Performance (optional)

Set these environment variables to enable request tracing:

- `SENTRY_DSN`
- `SENTRY_TRACES_SAMPLE_RATE` (e.g. `0.1`)
- `SENTRY_ENVIRONMENT` (e.g. `development`, `production`)
